# 카카오 지도 인증 설정

이 브랜치는 카카오 지도 SDK, 키워드 장소 검색, 주소→좌표·좌표→주소 변환, 도보 경로만 사용합니다. 카카오 로그인이나 카카오내비 앱 연동은 필요하지 않습니다. 친구 연결은 기존 Apple CloudKit입니다.

## 1. 카카오디벨로퍼스 앱 생성

[카카오디벨로퍼스](https://developers.kakao.com/)에 로그인해 **앱 → 앱 생성**에서 Reunion 앱을 만듭니다. 기존 테스트 앱을 사용해도 됩니다.

## 2. 카카오맵 활성화

앱 관리의 **카카오맵 → 사용 설정 → 상태 ON**을 선택합니다. 개발자 계정에서 처음 활성화한 앱에 무료 쿼터가 제공됩니다. 두 번째 앱부터 또는 무료 쿼터 초과 사용은 유료 API 설정이 필요할 수 있습니다. 무료 범위로 테스트하려면 무료 쿼터 적용 앱을 확인하고 유료 사용을 임의로 켜지 마세요. 실제 쿼터와 사용량은 콘솔에서 확인합니다.

## 3. 키 발급과 iOS 등록

**앱 설정 → 앱 → 플랫폼 키**에서 다음 두 키를 확인하거나 발급합니다.

| 키 | 사용처 | 설정 |
| --- | --- | --- |
| 네이티브 앱 키 | iOS 카카오 지도 표시 | iOS 앱 정보에 번들 ID `com.reunion.poc` 등록 |
| REST API 키 | 키워드 검색, 주소/좌표 변환, 도보 경로 | 카카오맵 REST API에 사용할 키의 사용 설정 확인 |

지도 SDK에 REST 키를 넣으면 안 됩니다. Admin 키, JavaScript 키, Client Secret은 이 앱 설정에 사용하지 않습니다. REST 키의 허용 IP를 제한한다면 iPhone의 이동통신·Wi-Fi IP가 달라지는 점을 고려해야 합니다.

## 4. 로컬 설정 파일에 입력

프로젝트의 `Config/Local.xcconfig`에는 아래 항목을 이미 만들어 두었습니다. 실제 키를 여기에 입력합니다. 이 파일은 Git에서 제외되고, 채팅으로 키를 보낼 필요는 없습니다.

```xcconfig
KAKAO_NATIVE_APP_KEY = 발급받은_네이티브_앱_키
KAKAO_REST_API_KEY = 발급받은_REST_API_키
```

변경 후 앱을 다시 빌드해야 합니다. 앱 안에서 키를 입력하거나 별도 서버를 배포할 필요는 없습니다. 과거 Google 키가 로컬 파일에 남아 있어도 이 브랜치는 사용하지 않습니다.

## 5. 실제 응답 확인

1. 함께 보기에서 현재 위치 지도 표시.
2. 장소 이름 또는 주소 검색 후 검색 결과의 핀 선택.
3. 지도 길게 누르기로 좌표에 해당하는 주소 확인.
4. 만날 시간 저장 후 현재 위치에서 도보 길찾기. 파란 경로·이동시간·거리·안내 문구 확인.
5. 다른 iCloud 계정의 친구를 초대하고 각 기기에서 위치 공유를 켜서 여러 핀과 상태를 확인.

지도 인증 실패는 네이티브 키·iOS 번들 ID·카카오맵 ON 상태를 먼저 확인합니다. 검색·길찾기 401/403은 REST 키 및 API 설정을, 429는 사용량을 확인합니다. 가능한 도보 경로가 없으면 오류를 표시하며 임의의 경로나 시간을 만들지 않습니다.

REST 키를 xcconfig에 넣어도 앱 바이너리에서 추출될 가능성은 남습니다. 현재는 개인 PoC 방식입니다. 광범위하게 배포하기 전에 키 보호와 호출량 관리를 별도로 검토해야 합니다.

## 공식 문서

- [카카오맵 사용 설정과 키 종류](https://developers.kakao.com/docs/ko/kakaomap/common)
- [카카오맵 REST API: 주소·검색·도보](https://developers.kakao.com/docs/ko/kakaomap/rest-api)
- [iOS 지도 SDK 사용 등록](https://apis.map.kakao.com/ios_v2/docs/getting-started/basics/02_auth/)
